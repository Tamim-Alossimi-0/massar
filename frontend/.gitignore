# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/
next.user-config.*

# Environment variables
.env*.local
# .env.local is intentionally committed so the production API URL is
# available at `next dev` time as well as in Vercel builds.
!.env.local

# Common ignores
node_modules
.next/
.DS_Store