# Core data
# numpy kept below 2.0 because torch 2.2.x and sentence-transformers 2.7
# pre-built wheels are still compiled against the numpy 1.x ABI.
pandas>=2.2.2
numpy>=1.26.3,<2.0
joblib

# NLP & ML
# Floors reflect the versions this project was developed against; pip is
# free to pick newer compatible releases during the HF Space build.
scikit-learn>=1.4.2
sentence-transformers>=2.7.0

# PyTorch — CUDA 12.1 wheel (RTX 4060 / any CUDA-capable GPU)
# Install via: pip install torch==2.2.2 --index-url https://download.pytorch.org/whl/cu121
# For CPU-only fallback: pip install torch==2.2.2 --index-url https://download.pytorch.org/whl/cpu
torch==2.2.2

# Nomic-embed-text-v1.5 requires einops for its custom model code
einops

# Web scraping
requests==2.31.0
beautifulsoup4==4.12.3
python-jobspy==1.1.82         # LinkedIn / Indeed / Glassdoor real-job collector

# PDF parsing
pdfplumber==0.11.0

# REST API backend (for Next.js frontend)
fastapi==0.135.3
uvicorn==0.43.0
python-multipart==0.0.9

# Testing
pytest==8.2.2
